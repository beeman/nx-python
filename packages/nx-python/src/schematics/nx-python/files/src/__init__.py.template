class HelloFactory:
    def __init__(self, **args):
        for key, value in args.items():
            setattr(self, key, 'Hello {}'.format(value))

hello = HelloFactory(application="<%= projectName %>", user="Human")

print(hello.application)